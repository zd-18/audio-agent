# Copy to infra/.env before running docker compose.
MINIO_ROOT_PASSWORD=replace-with-a-strong-local-password
# Optional absolute host directory. Leave unset to use the named Docker volume.
# MINIO_DATA_DIR=D:/audio-agent-data/minio
